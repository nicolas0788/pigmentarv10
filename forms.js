/*document.addEventListener("DOMContentLoaded", function () {
  const email = {
    email: "",
    categoria: "",
    nombre: "",
    apellido: "",
    telefono: "",
    textarea: "",
  };

  const inputEmail = document.getElementById("email");
  const inputNombre = document.getElementById("nombre");
  const inputApellido = document.getElementById("apellido");
  const inputTelefono = document.getElementById("telefono");
  const inputCategoria = document.getElementById("categoria");
  const inputMensaje = document.getElementById("textarea");

  inputEmail.addEventListener("blur", obtenerValor);
  inputNombre.addEventListener("blur", obtenerValor);
  inputApellido.addEventListener("blur", obtenerValor);
  inputTelefono.addEventListener("blur", obtenerValor);
  inputCategoria.addEventListener("blur", obtenerValor);
  inputMensaje.addEventListener("blur", obtenerValor);

  function obtenerValor(e) {
    email[e.target.name] = e.target.value.trim().toLowerCase();
    console.log(email);
  }

  console.log(email);
});*/
